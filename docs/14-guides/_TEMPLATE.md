# [Nombre del Cliente] con API LLM Gateway

## Descripción General
[Breve descripción del cliente y cómo se beneficia al conectarse al Gateway (ej: acceso a múltiples proveedores, failover automático, enrutamiento por capacidades).]

## Prerrequisitos
- API LLM Gateway en ejecución (por defecto `http://localhost:8080`).
- API Key del Gateway válida (si la autenticación está habilitada).
- [Cliente] versión [X.Y.Z] o superior.

## Configuración Paso a Paso

### 1. Endpoint y Autenticación
[Instrucciones exactas sobre dónde y cómo configurar la Base URL y el API Key en la interfaz gráfica o variables de entorno del cliente].

### 2. Selección de Modelos y Capacidades
[Cómo aprovechar el enrutamiento dinámico del Gateway, p.ej. usando `router:capability:chat` o prefijos mágicos].

## Ejemplos Prácticos

### Usando cURL
```bash
# Comando de ejemplo para validar que el cliente/Gateway se hablan
```

### Configuración en Código (Si aplica)
```python
# Código de inicialización del SDK
```

## Solución de Problemas Comunes
- **Síntoma 1**: Causa y solución.
- **Síntoma 2**: Causa y solución.
